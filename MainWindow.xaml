<Window x:Class="MyAboutBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MyAboutBox"
        Title="MyAboutBox" Height="240" Width="320">
    <!--<Window.CommandBindings>
        <CommandBinding Command="{x:Static local:MainWindow.Exit}" Executed="ExitHandler" CanExecute="ExitCanExecute" />
    </Window.CommandBindings>-->
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <MenuItem Header="_File">
                <!--<MenuItem Command="Exit" />-->
                <MenuItem Header="E_xit" Click="Exit_Click" />
            </MenuItem>
            <MenuItem Header="_Help">
                <!--<MenuItem Command="About" />-->
                <MenuItem Header="About" Click="About" />
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="Ready" />
        </StatusBar>
        <Grid>
        </Grid>
    </DockPanel>
</Window>
